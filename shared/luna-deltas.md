# Luna Delta Ledger

## Baseline

**Status:** No behavioral delta at repository initialization.

`minmax-orchestrator-luna` is materialized from the current validated `minmax-orchestrator-next` implementation. The initial differences are limited to distribution identity and model-specific naming/metadata.

## Future delta template

For every intentional Luna-specific divergence, append:

```text
Delta ID:
Date:
NEXT baseline:
Files/components:
Observed Luna behavior:
Evidence/eval:
Change:
Expected gain:
Added cost/risk:
Regression tests:
Upstream candidate?: yes/no
```

Do not record ordinary upstream synchronization as a Luna delta.
