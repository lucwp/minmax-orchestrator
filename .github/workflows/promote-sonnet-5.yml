name: Promote Sonnet 5 stable release

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - '.github/workflows/promote-sonnet-5.yml'

permissions:
  contents: write

jobs:
  promote:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Install test runner
        run: python -m pip install pytest
      - name: Materialize stable Sonnet 5
        run: python tools/materialize_sonnet_5.py
      - name: Validate existing stable distributions
        run: python tools/validate_all.py
      - name: Validate Sonnet 5
        run: python -m pytest -q
        working-directory: minmax-orchestrator-sonnet-5
      - name: Clean generated caches
        run: |
          find minmax-orchestrator-sonnet-5 -type d -name '__pycache__' -prune -exec rm -rf {} +
          find minmax-orchestrator-sonnet-5 -type d -name '.pytest_cache' -prune -exec rm -rf {} +
      - name: Commit stable release
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add -A minmax-orchestrator-sonnet-5
          if git diff --cached --quiet; then
            echo 'Sonnet 5 release already materialized.'
            exit 0
          fi
          git commit -m 'Add stable Sonnet 5 orchestrator release'
          git push
