import importlib.util,json
from pathlib import Path
ROOT=Path(__file__).resolve().parents[1];spec=importlib.util.spec_from_file_location('grader',ROOT/'scripts'/'grade_behavioral_observations.py');g=importlib.util.module_from_spec(spec);spec.loader.exec_module(g);vspec=importlib.util.spec_from_file_location('validator',ROOT/'scripts'/'validate_loop_contract.py');v=importlib.util.module_from_spec(vspec);vspec.loader.exec_module(v);cases=json.loads((ROOT/'evals'/'loop-mode'/'cases.json').read_text(encoding='utf-8'))['cases'];case=next(c for c in cases if c['id']=='subjective_design');contract=json.loads((ROOT/'evals'/'loop-mode'/'golden'/'en-complex.json').read_text(encoding='utf-8'));contract['topology']='evaluator_optimizer';contract['side_effect_class']='reversible_write';contract['state']={'mode':'checkpointed','store':'grader-checkpoint.json','version_marker':'1.0','resume_strategy':'validate checkpoint and authoritative state before continuing'};contract['complexity_gate']['decision']='escalate';contract['complexity_gate']['benefit_types']=['iterative_critique_gain'];contract['verification']['method']='bounded_semantic_rubric';contract['verification']['evidence_source']='independent_rubric';contract['approval']={'status':'pending'};contract['contract_digest']=v.compute_contract_digest(contract);assert not g.grade_case(case,contract);bad=json.loads(json.dumps(contract));bad['topology']='orchestrator_workers';bad['contract_digest']=v.compute_contract_digest(bad);assert g.grade_case(case,bad);results=g.grade_observations(cases,{},allow_partial=False);assert len(results)==len(cases) and all(errs for _,errs in results)
def test_test_behavioral_observation_grader_module_regression_loaded():assert True
